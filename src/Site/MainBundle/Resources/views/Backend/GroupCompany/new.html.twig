{% extends 'SiteMainBundle:Backend:layout.html.twig' %}

{% block content -%}
    <h1>{{ 'backend.group_company.h1_create'|trans({}, 'menu') }}</h1>

    {{ form(form, { 'style': 'horizontal', attr: {novalidate: 'novalidate'} }) }}

    <ul class="record_actions nav nav-pills">
        <li>
            <a href="{{ path('backend_group_company_index') }}">
                {{ 'backend.group_company.back_to_list'|trans({}, 'menu') }}
            </a>
        </li>
    </ul>
{% endblock %}